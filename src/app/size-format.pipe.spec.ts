import { SizeFormatPipe } from './size-format.pipe';

describe('SizeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
